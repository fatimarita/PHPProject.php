{% extends 'base.twig' %}
{% block body %}
    <h1>{{title}}</h1>
    <p>Description : {{ products[0]["description"] }}</p>
    {% if title == "Bonjour !" %}
       <p>Bonjour</p>
    {% elseif title == "Bonsoir !" %}
      <p>Bonsoir</p>
      {% else %}
      <p>Bonne nuit</p>
      {% endif %}
      {% for produitsEnCours in products %}
      <p>{{ produitEnCours['name'] }}</p>
      {#   Commentaire multiligne twig php   #}
      {% endblock %}
